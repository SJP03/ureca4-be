# =============================================================================
# LG U+ 청구서 시스템 환경 변수 설정
# =============================================================================
# ⚠️ 이 파일을 복사하여 .env 파일을 생성해야 합니다 (필수!)
# 
# 사용법:
# 1. 이 파일을 복사: .env.local & .env.prod 생성
# 2. .env 파일에 실제 값 입력 (Slack에서 공유)
# 3. .env 파일은 절대 Git에 커밋하지 마세요!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (MySQL) 
# -----------------------------------------------------------------------------
DB_URL=jdbc:mysql://[서버주소]:[포트]/[DB명]?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
DB_USERNAME=[사용자명]
DB_PASSWORD=[비밀번호]

# -----------------------------------------------------------------------------
# Redis Configuration 
# -----------------------------------------------------------------------------
REDIS_HOST=[서버주소]
REDIS_PORT=[포트]

# -----------------------------------------------------------------------------
# Kafka Configuration 
# -----------------------------------------------------------------------------
KAFKA_SERVERS=[서버주소]:[포트]

# -----------------------------------------------------------------------------
# Security - Jasypt Encryption Key - 필수
# -----------------------------------------------------------------------------
# 개인정보 암호화에 사용되는 키
JASYPT_PASSWORD=[암호화키]
AES_SECRET_KEY=[암호화키]
HASH_SECRET_KEY=[암호화키]

# -----------------------------------------------------------------------------
# Gmail 앱 비밀번호 생성 방법 (테스트용)
# -----------------------------------------------------------------------------
# 이메일 발송 기능을 테스트하려면 개인 Gmail 앱 비밀번호가 필요합니다.
# 공용 gmail notion key page에 있음
MAIL_USERNAME=[Gmail 주소]
MAIL_PASSWORD=[Gmail 앱 비밀번호]